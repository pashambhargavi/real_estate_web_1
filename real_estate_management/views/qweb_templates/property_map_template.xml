<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="property_map_template" name="Property Map Page">
        <t t-call="website.layout">

            <t t-set="head">
                <link rel="stylesheet" href="/real_estate_management/static/src/css/property_map.css"/>
                <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"/>
                <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"/>
                <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"/>
            </t>

            <div id="wrap">
                <main class="container-fluid">

                    <!-- TOP BAR: Single line — [City Filter] [Heading] [Subtitle] -->
                    <section id="top-bar" class="row">
                        <div class="col-12 top-bar-inner">

                            <!-- LEFT: Label + Dropdown inline -->
                            <div class="left-block">
                                <div class="city-filter-container">
                                    <form method="get" action="" id="cityFilterForm">
                                        <label for="citySelect">
                                            <i class="fas fa-map-marker-alt"></i>
                                            Select City:
                                        </label>
                                        <select name="city" id="citySelect" class="form-select"
                                                onchange="this.form.submit();">
                                            <option value="">All Cities</option>
                                            <t t-foreach="city_list" t-as="city">
                                                <option t-att-value="city"
                                                        t-att-selected="'selected' if city==selected_city else None">
                                                    <t t-esc="city"/>
                                                </option>
                                            </t>
                                        </select>
                                    </form>
                                </div>
                            </div>

                            <!-- CENTER: Main heading -->
                            <div class="center-block">
                                <h1 class="main-heading">Explore Our Exclusive Property Showcase</h1>
                            </div>

                            <!-- RIGHT: Subtitle -->
                            <div class="right-block">
                                <h2 class="right-subtitle">
                                    Premium Properties
                                    <t t-if="selected_city">
                                        in <span class="city-name"><t t-esc="selected_city"/></span>
                                    </t>
                                    <t t-else="">in All Cities</t>
                                </h2>
                            </div>

                        </div>
                    </section>

                    <!-- TICKER ANCHOR -->
                    <div id="ai-ticker-anchor"></div>

                    <!-- MAP -->
                    <section id="map-display" class="row">
                        <div class="col-12">
                            <div id="category-legend" t-att-data-colors="category_colors"></div>
                            <div id="propertyMap"></div>
                        </div>
                    </section>

                    <!-- FEATURED PROPERTIES -->
                    <t t-if="selected_city">
                        <section id="featured-properties" class="featured-section">
                            <div class="container-fluid px-0">
                                <div class="section-header text-center">
                                    <h2 class="section-title">
                                        <span class="title-accent">&#x2728;</span>
                                        Featured Properties in
                                        <span class="city-highlight"><t t-esc="selected_city"/></span>
                                        <span class="title-accent">&#x1F3E0;</span>
                                    </h2>
                                    <p class="section-subtitle">Discover our handpicked premium properties with exceptional value</p>
                                </div>
                                <div class="property-grid">
                                    <t t-foreach="featured_properties" t-as="fp">
                                        <div class="property-card-wrapper">
                                            <div class="property-card h-100">
                                                <a t-att-href="'/property/%d' % fp.id" class="card-link">
                                                    <div class="image-container">
                                                        <img t-if="fp.image"
                                                             t-att-src="'data:image/png;base64,%s' % fp.image.decode('utf-8')"
                                                             t-att-alt="fp.name"
                                                             class="property-image"/>
                                                        <div class="image-overlay d-flex align-items-center justify-content-center">
                                                            <div class="overlay-content">
                                                                <span class="view-details">View Details</span>
                                                                <i class="fas fa-arrow-right ms-2"></i>
                                                            </div>
                                                        </div>
                                                        <div class="price-badge">
                                                            &#x20B9; <t t-esc="'{:,}'.format(int(fp.price or 0))"/>
                                                        </div>
                                                    </div>
                                                    <div class="card-content">
                                                        <h3 class="property-name"><t t-esc="fp.name"/></h3>
                                                        <div class="property-location d-flex align-items-center mb-3">
                                                            <i class="fas fa-map-marker-alt me-2"></i>
                                                            <span><t t-esc="fp.city"/></span>
                                                        </div>
                                                        <div class="property-features d-flex flex-wrap gap-2 mb-3">
                                                            <div class="feature d-flex align-items-center" t-if="fp.plot_area">
                                                                <i class="fas fa-expand-arrows-alt me-1"></i>
                                                                <span><t t-esc="int(fp.plot_area)"/> sq.ft</span>
                                                            </div>
                                                            <div class="feature d-flex align-items-center" t-if="fp.category_id">
                                                                <i class="fas fa-home me-1"></i>
                                                                <span><t t-esc="fp.category_id.name"/></span>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </a>
                                            </div>
                                        </div>
                                    </t>
                                </div>
                            </div>
                        </section>
                    </t>

                    <!-- INVESTMENT OVERVIEW -->
                    <section id="investment-overview" class="mt-3">
                        <div class="container text-center">
                            <t t-if="selected_city">
                                <div class="mb-5">
                                    <h2>Why Should You Invest in
                                        <span class="highlight-city"><t t-esc="selected_city"/></span>?
                                    </h2>
                                    <p class="text-muted">Discover expert insights backed by growth, connectivity, and trust.</p>
                                </div>
                                <div class="row g-4">
                                    <div class="col-md-6 col-lg-3">
                                        <div class="info-box info-box-green">
                                            <h5 class="fw-bold info-title mb-2">Investment Reasons</h5>
                                            <p class="info-text"
                                               t-out="city_investment_info and city_investment_info.get('ai_investment_reasons') or ''"/>
                                        </div>
                                    </div>
                                    <div class="col-md-6 col-lg-3">
                                        <div class="info-box info-box-blue">
                                            <h5 class="fw-bold info-title mb-2">Growth Potential</h5>
                                            <p class="info-text"
                                               t-out="city_investment_info and city_investment_info.get('ai_growth_potential') or ''"/>
                                        </div>
                                    </div>
                                    <div class="col-md-6 col-lg-3">
                                        <div class="info-box info-box-yellow">
                                            <h5 class="fw-bold info-title mb-2">Infrastructure</h5>
                                            <p class="info-text"
                                               t-out="city_investment_info and city_investment_info.get('ai_infrastructure') or ''"/>
                                        </div>
                                    </div>
                                    <div class="col-md-6 col-lg-3">
                                        <div class="info-box info-box-red">
                                            <h5 class="fw-bold info-title mb-2">Market Trends</h5>
                                            <p class="info-text"
                                               t-out="city_investment_info and city_investment_info.get('ai_market_trends') or ''"/>
                                        </div>
                                    </div>
                                </div>
                            </t>
                            <t t-else="">
                                <div class="p-5 overview-empty">
                                    <h3 class="fw-semibold empty-title">Select a City to Discover Smart Investment Insights!</h3>
                                    <p class="empty-subtitle">Choose a city from the dropdown to see AI-generated real estate highlights.</p>
                                </div>
                            </t>
                        </div>
                    </section>

                </main>
            </div>

            <!-- HIDDEN DATA for map JS -->
            <section id="hidden-data">
                <div id="property-data" t-att-data-properties="properties_json"/>
            </section>

        </t>
    </template>
</odoo>